[build]
  command = "npm run build"
  publish = "dist"

# Optional: local development settings (Netlify CLI)
[dev]
  command = "npm run dev"
  port = 8888
  framework = "vite"

# Ensure single-page app routes work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
